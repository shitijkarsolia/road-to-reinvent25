# Connection Info

The backend runs locally. Start it with:

```bash
cd backend
source venv/bin/activate
uvicorn app:app --host 0.0.0.0 --port 8000
```

Then open `http://localhost:3000` for the frontend.

See `README.md` for full setup instructions.
